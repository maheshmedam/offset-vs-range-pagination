## Comparing Offset based vs Range based Pagination

### Running the code
1. Change directory to the project.
2. Expects MongoDB server to be running on `http://localhost:27017`
3. Run with `go run main.go`

